{
  "name": "@evansdai/trellis-pi-ext",
  "version": "0.1.0",
  "description": "Repository-owned Pi extension fork providing Trellis task-context injection, startup/session lifecycle behavior, and pi-subagents child-session suppression.",
  "type": "module",
  "main": "./index.ts",
  "scripts": {
    "test": "tsx --test test/*.test.ts",
    "typecheck": "tsc -p tsconfig.json",
    "check": "npm run typecheck && npm test"
  },
  "pi": {
    "extensions": ["./index.ts"]
  },
  "license": "AGPL-3.0-only",
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "0.83.0",
    "@types/node": "^22.10.0",
    "jiti": "^2.7.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
